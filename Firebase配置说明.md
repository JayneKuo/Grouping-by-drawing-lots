# Firebase自动同步配置说明

## 🎯 实现自动数据同步，无需导出/导入！

使用Firebase Firestore可以实现：
- ✅ **自动同步** - 所有用户数据自动同步
- ✅ **实时更新** - 一个用户修改，其他用户立即看到
- ✅ **无需导出** - 不需要手动导出/导入文件
- ✅ **完全免费** - Firebase免费套餐足够使用

## 🚀 3步快速配置

### 步骤1：创建Firebase项目

1. 访问 https://console.firebase.google.com/
2. 点击 **"添加项目"** 或 **"Create a project"**
3. 输入项目名称（如：`tennis-tournament`）
4. 点击 **"继续"**
5. 可以选择是否启用Google Analytics（建议选择"不启用"）
6. 点击 **"创建项目"**
7. 等待项目创建完成

### 步骤2：获取配置信息

1. 在Firebase控制台，点击左侧的 **⚙️ 项目设置**（Project Settings）
2. 滚动到 **"你的应用"**（Your apps）部分
3. 点击 **"Web"** 图标（`</>`）
4. 输入应用昵称（如：`tennis-tournament-web`）
5. 点击 **"注册应用"**
6. 复制配置信息（类似下面的格式）：

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
}
```

### 步骤3：配置Firestore数据库

1. 在Firebase控制台，点击左侧的 **"Firestore Database"**
2. 点击 **"创建数据库"**
3. 选择 **"以测试模式启动"**（开发阶段）
4. 选择区域（建议选择 `asia-east1` 或 `asia-southeast1`）
5. 点击 **"启用"**

### 步骤4：修改代码配置

1. 打开 `src/utils/firebase.js` 文件
2. 将步骤2中复制的配置信息替换到 `firebaseConfig` 中：

```javascript
const firebaseConfig = {
  apiKey: "你的API_KEY",
  authDomain: "你的项目ID.firebaseapp.com",
  projectId: "你的项目ID",
  storageBucket: "你的项目ID.appspot.com",
  messagingSenderId: "你的发送者ID",
  appId: "你的应用ID"
}
```

### 步骤5：完成！

保存文件后，系统会自动使用Firebase同步数据！

## ✅ 完成后的效果

- ✅ 所有用户的数据自动同步
- ✅ 一个用户创建比赛，其他用户立即看到
- ✅ 一个用户添加选手，其他用户立即看到
- ✅ 一个用户录入比分，其他用户立即看到
- ✅ **无需任何导出/导入操作！**

## 💡 工作原理

1. **数据存储**：所有数据存储在Firebase Firestore中
2. **自动同步**：Firebase自动处理数据同步
3. **实时更新**：使用Firebase的实时监听功能，数据变化立即推送
4. **降级处理**：如果Firebase未配置，自动降级到localStorage

## 📋 Firebase免费套餐限制

- **存储**：1GB（足够存储数千场比赛）
- **读取**：每天50,000次（足够日常使用）
- **写入**：每天20,000次（足够日常使用）
- **网络**：10GB/月（足够使用）

对于小型比赛（<100场比赛），完全够用！

## ⚠️ 安全设置（可选）

如果需要限制访问，可以设置Firestore安全规则：

1. 在Firebase控制台，点击 **"Firestore Database"** → **"规则"**
2. 修改规则为：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tennis_tournaments/{document=**} {
      allow read, write: if true; // 开发阶段允许所有人读写
    }
  }
}
```

**注意**：生产环境应该设置更严格的规则！

## 🔧 如果不想使用Firebase

如果不想配置Firebase，系统会自动降级到localStorage模式：
- 数据存储在浏览器中
- 需要通过导出/导入文件同步
- 功能完全正常，只是没有自动同步

## 📞 如果遇到问题

1. **配置错误**：检查 `firebase.js` 中的配置是否正确
2. **权限错误**：检查Firestore安全规则是否允许读写
3. **网络错误**：检查网络连接，Firebase需要网络访问

## 🎉 完成！

配置完成后，所有用户的数据会自动同步，无需任何手动操作！

