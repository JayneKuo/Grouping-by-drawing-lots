# 球搭子网球赛事管理系统 - 安装说明

## 📋 技术栈

- **前端**: Vue3 + Vant + Vue Router + Pinia + Vite
- **后端**: Node.js + Express + MySQL2
- **数据库**: MySQL
- **缓存**: localStorage（前端）

## 🚀 快速开始

### 1. 安装依赖

```bash
# 安装前端依赖
npm install

# 安装后端依赖
cd backend
npm install
cd ..
```

### 2. 配置数据库

```bash
# 创建MySQL数据库（需要先安装MySQL）
mysql -u root -p < backend/config/db.sql

# 或手动创建数据库
mysql -u root -p
CREATE DATABASE tennis_tournament CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE tennis_tournament;
SOURCE backend/config/db.sql;
```

### 3. 配置环境变量

```bash
# 复制环境变量模板
cp backend/.env.example backend/.env

# 编辑 backend/.env 文件，填入数据库配置
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your_password
# DB_NAME=tennis_tournament
```

### 4. 启动项目

```bash
# 方式1：同时启动前端和后端（推荐）
npm start

# 方式2：分别启动
npm run dev          # 前端开发服务器 (http://localhost:8080)
npm run server       # 后端API服务器 (http://localhost:3000)
```

## 📁 项目结构

```
tennis-tournament-manager/
├── src/                    # Vue3前端源码
│   ├── main.js            # 入口文件
│   ├── App.vue            # 根组件
│   ├── router/            # 路由配置
│   │   └── index.js
│   ├── store/             # Pinia状态管理
│   │   └── user.js
│   ├── views/             # 页面组件
│   │   ├── Home.vue       # 首页
│   │   ├── Login.vue      # 登录页
│   │   ├── Tournaments.vue # 比赛列表
│   │   ├── TournamentDetail.vue # 比赛详情
│   │   ├── MatchScore.vue # 比分录入
│   │   └── Display.vue    # 大屏展示
│   ├── components/        # 公共组件
│   │   └── CreateTournament.vue
│   ├── api/               # API接口
│   │   ├── request.js     # Axios封装
│   │   ├── auth.js        # 认证API
│   │   └── tournaments.js # 比赛API
│   └── utils/             # 工具函数
├── backend/               # Express后端
│   ├── server.js          # 服务器入口
│   ├── config/            # 配置文件
│   │   ├── database.js    # 数据库配置
│   │   └── db.sql         # 数据库SQL
│   ├── routes/            # 路由
│   │   ├── auth.js        # 认证路由
│   │   ├── tournaments.js # 比赛路由
│   │   ├── matches.js     # 比赛记录路由
│   │   └── scores.js      # 比分路由
│   └── middleware/        # 中间件
│       └── auth.js        # 认证中间件
├── index.html             # HTML入口
├── vite.config.js         # Vite配置
└── package.json           # 依赖配置
```

## 🔧 开发说明

### 前端开发

- 使用Vue3 Composition API
- 使用Vant组件库（移动端UI）
- 使用Pinia进行状态管理
- 使用Vue Router进行路由管理
- 使用Axios进行HTTP请求

### 后端开发

- Express框架
- MySQL数据库
- JWT认证
- RESTful API设计

### 数据库设计

- users: 用户表
- tournaments: 比赛表
- players: 选手表
- matches: 比赛记录表
- sets: 盘分记录表
- scores: 比分记录表（逐分）
- operation_logs: 操作日志表

## 📱 功能模块

### 已完成
- ✅ Vue3项目结构
- ✅ Vant组件库集成
- ✅ 路由配置
- ✅ 状态管理（Pinia）
- ✅ 登录/注册页面
- ✅ 比赛列表页面
- ✅ 比赛创建页面
- ✅ 后端API基础结构
- ✅ MySQL数据库设计

### 待完成
- ⏳ 比分录入页面（逐分录入）
- ⏳ 积分榜页面
- ⏳ 大屏展示页面（16:9/4:3适配）
- ⏳ localStorage缓存机制
- ⏳ 完整的后端API实现
- ⏳ 实时同步功能

## 🆘 常见问题

### Q: 数据库连接失败？
A: 检查 `backend/.env` 文件中的数据库配置是否正确

### Q: 端口被占用？
A: 修改 `vite.config.js` 和 `backend/server.js` 中的端口号

### Q: 前端无法访问后端API？
A: 检查 `vite.config.js` 中的proxy配置

## 📝 下一步

1. 完善比分录入功能
2. 实现积分榜计算
3. 实现大屏展示（16:9/4:3适配）
4. 实现localStorage缓存
5. 完善后端API

