# 🔐 Firebase Firestore 权限配置说明

## ⚠️ 重要：必须配置Firebase权限才能多用户共享数据

当前问题：
- ❌ 别人看不到你创建的比赛
- ❌ 刷新后数据丢失

**原因**：Firebase Firestore默认不允许未授权访问，需要配置权限规则。

## 🚀 快速配置步骤

### 步骤1：打开Firebase控制台

访问：https://console.firebase.google.com/project/tennis-tournament-f2e6e/firestore

### 步骤2：配置Firestore规则

1. 点击左侧菜单 **"Firestore Database"**
2. 点击顶部标签 **"规则"** (Rules)
3. 将以下规则复制粘贴进去：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 允许所有人读写tennis_tournaments集合
    match /tennis_tournaments/{document=**} {
      allow read, write: if true;
    }
  }
}
```

4. 点击 **"发布"** (Publish) 按钮

### 步骤3：验证配置

配置完成后，所有用户都可以：
- ✅ 读取数据（看到所有比赛）
- ✅ 写入数据（创建/修改比赛）
- ✅ 数据实时同步

## 📝 规则说明

上面的规则允许：
- **所有人**可以读取和写入 `tennis_tournaments` 集合
- 这是**公开数据**，适合比赛管理系统

## 🔒 如果需要更安全的配置（可选）

如果你希望限制访问，可以使用：

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /tennis_tournaments/{document=**} {
      // 允许所有人读取，但只有认证用户才能写入
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

但对于当前的使用场景，**公开读写**是最简单的方案。

## ✅ 配置完成后

1. 重新部署应用（如果需要）
2. 清除浏览器缓存
3. 刷新页面
4. 创建比赛，其他人应该能看到

## 🐛 如果还是不行

1. 检查浏览器控制台是否有Firebase错误
2. 确认Firebase项目ID是否正确：`tennis-tournament-f2e6e`
3. 确认Firestore已启用（在Firebase控制台检查）

