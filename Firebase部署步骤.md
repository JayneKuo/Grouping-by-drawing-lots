# 🚀 Firebase Hosting 部署步骤

## 📋 部署前准备

### 1. 安装Firebase CLI工具

```bash
npm install -g firebase-tools
```

### 2. 登录Firebase

```bash
firebase login
```

这会打开浏览器，让你登录Google账号。

## 🚀 部署步骤

### 步骤1：构建项目

首先需要构建Vue项目：

```bash
npm run build
```

这会生成 `dist` 目录，包含所有静态文件。

### 步骤2：初始化Firebase项目（如果还没初始化）

```bash
firebase init hosting
```

**配置选项：**
- **选择项目**：选择 `tennis-tournament-f2e6e`
- **公共目录**：输入 `dist`（这是Vite构建后的输出目录）
- **单页应用**：选择 `Yes`（因为Vue Router需要）
- **自动覆盖**：选择 `No`（保留现有文件）

### 步骤3：部署到Firebase Hosting

```bash
firebase deploy --only hosting
```

## ✅ 部署完成

部署完成后，你会看到：
- **部署URL**：`https://tennis-tournament-f2e6e.web.app`
- **备用URL**：`https://tennis-tournament-f2e6e.firebaseapp.com`

## 📝 快速部署命令（一键部署）

我已经创建了配置文件，你可以直接运行：

```bash
# 1. 构建项目
npm run build

# 2. 部署到Firebase
firebase deploy --only hosting
```

## 🔄 更新部署

以后每次更新代码后，只需要：

```bash
npm run build
firebase deploy --only hosting
```

## ⚙️ 配置文件说明

我已经创建了以下配置文件：

### `firebase.json`
- 配置了部署目录为 `dist`
- 配置了单页应用路由重写规则

### `.firebaserc`
- 配置了项目ID：`tennis-tournament-f2e6e`

## 💡 注意事项

1. **首次部署**：需要先运行 `firebase login` 登录
2. **构建**：每次部署前都需要运行 `npm run build`
3. **自动部署**：可以设置GitHub Actions自动部署（可选）

## 🐛 如果遇到问题

### 问题1：firebase命令不存在
- 确保已安装：`npm install -g firebase-tools`
- 检查PATH环境变量

### 问题2：登录失败
- 检查网络连接
- 尝试使用：`firebase login --no-localhost`

### 问题3：部署失败
- 检查 `dist` 目录是否存在
- 确认已运行 `npm run build`
- 检查 `firebase.json` 配置是否正确

## 🎉 完成！

部署完成后，你的应用就可以通过以下URL访问：
- https://tennis-tournament-f2e6e.web.app
- https://tennis-tournament-f2e6e.firebaseapp.com

所有用户都可以访问，数据会自动同步！

